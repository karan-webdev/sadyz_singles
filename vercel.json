{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "regions": ["sfo1"],
  "env": {
    "STRIPE_SK_KEY": {
      "description": "Stripe Secret Key for payment processing"
    },
    "STRIPE_WEBHOOK_SECRET": {
      "description": "Stripe Webhook Signing Secret for webhook verification"
    },
    "RESEND_API_KEY": {
      "description": "Resend API Key for sending emails"
    },
    "VITE_SEASON_PRODUCT_ID": {
      "description": "Stripe Product ID for Season Pass"
    },
    "VITE_MULTI_PRODUCT_ID": {
      "description": "Stripe Product ID for Weekly Pass"
    },
    "APP_URL": {
      "description": "Application URL (optional, uses VERCEL_URL by default)"
    }
  }
}
